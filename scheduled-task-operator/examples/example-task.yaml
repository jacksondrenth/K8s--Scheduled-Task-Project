apiVersion: ops.io/v1
kind: ScheduledTask
metadata:
  name: hello-world
  namespace: default
spec:
  schedule: "*/5 * * * *"
  image: busybox
  command: ["echo", "hello from scheduled task operator"]
  suspended: false
  jitter: 120