apiVersion: ops.io/v1
kind: ScheduledTask
metadata:
  name: morning-song
  namespace: default
spec:
  schedule: "0 14 * * *"
  image: morning-song:latest
  command: ["python", "morning_song.py"]
  suspended: false
  jitter: 0